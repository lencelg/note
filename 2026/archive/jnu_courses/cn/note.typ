#import "@preview/ori:0.2.5": *

#set heading(numbering: numbly("{1:一}、", default: "1.1  "))
#set math.equation(numbering: "(1)")

#show: ori.with(
  title: "计算机网络考点笔记",
  author: "lencelg from Arcadai Bay",
  semester: "2026 summer",
  date: datetime.today(),
  maketitle: true,
  size: 10pt,
  font: (
    main: "Noto Sans CJK SC",
    mono: "IBM Plex Mono",
    cjk: "Noto Sans CJK SC",
    emph-cjk: "Noto Sans CJK SC",
    math-cjk: "Noto Sans CJK SC"
  )
)
#set text(weight: 500)

#outline()

#pagebreak()

= 第一章: 概述

== 互联网的两个基本特点

- 连通性 (connectivity)
- 资源共享 (sharing)

== 概念辨析

1. *我们家宽带欠费，断网了*  
   *解析*：宽带断了，意味着失去了连接全球公共互联网（外网）的链路，无法访问百度、微信等外部资源。

2. *好兄弟，要不要一起去上网*  
   *解析*：去网吧“上网”，核心目的是接入公共互联网玩游戏、看视频、聊天，指代的同样是全球公共网络。

3. *想把手机投屏到电视上，需要将手机、电视连接到同一网络*  
   *解析*：这里的“同一网络”特指局域网（LAN）（如家里的Wi-Fi）。投屏数据通常在本地路由器内部传输，不经过外网，因此它属于最基础的“计算机网络”范畴（不一定是“互连网”，更不一定是“互联网”）。

4. *银行工作人员需要登录银行内网才能给客户办理业务*  
   *解析*：银行内网是一个封闭的、私有的网络系统。它将银行内部的众多服务器和终端互联，但它不属于公共“互联网（Internet）”。它是“多个网络互联”形成的专用网络，因此对应“互连网”概念（在行业中常称为 Intranet）。

== 计算机通信

计算机通信实际上是主机A的某个进程与主机B的某个进程进行通信。

端系统的两种通信：

- Client/Server
- P2P

典型的交换技术：

- 电路交换
- 报文交换
- 分组交换

=== 电路交换

*电路交换（Circuit Switching）*：
- 通过物理线路的连接，动态地分配传输线路资源。
- 通话的两个用户*始终*占用端到端的通信资源
- 电线对的数量与电话机数量的平方 $N^2$ 成正比。

分为三个阶段：
- 建立连接：建立一条专用的物理通路（占用通信资源）。
- 通话：主叫和被叫双方互相通电话（一直占用通信资源）。
- 释放连接：释放刚才使用的专用的物理通路（归还通信资源）。

*电路交换的优点：*
- 通信前从主叫端到被叫端建立一条专用的物理通路，在通信的全部时间内，两个用户始终占用端到端的线路资源。数据直送，传输速率高。
- 电路交换更适用于：低频次、大量地传输数据。

*电路交换的缺点：*
- 建立/释放连接，需要额外的时间开销。
- 线路被通信双方独占，利用率低。
- 线路分配的灵活性差。
- 交换节点*不支持“差错控制”*

=== 报文交换

*报文交换的优点：*
- 通信前无需建立连接
- 数据以“报文”为单位被交换节点间“存储转发”，通信线路可以灵活分配
- 在通信时间内，两个用户无需独占一整条物理线路。相比于电路交换，线路利用率高
- 交换节点支持“差错控制”（通过校验技术）

*报文交换的缺点：*
- 报文不定长，不方便存储转发管理
- 长报文的存储转发时间开销大，缓存开销大
- 长报文容易出错，重传代价高

=== 分组交换

*分组*是在互联网中传送的*数据单元*。

分组在互联网的交换过程（有助于理解互联网）
- 根据首部中包含的目的地址、源地址等重要控制信息进行转发。
- 每一个分组在互联网中独立选择传输路径。
- 位于网络核心部分的路由器负责转发分组，即进行分组交换。
- 路由器要创建和动态维护转发表。

分组交换的优点：
- 通信前 *无需* 建立连接  
- 数据以 *分组* 为单位被交换节点间 *存储转发*，通信线路可以灵活分配  
- 在通信时间内，两个用户无需独占一整条物理线路。相比于电路交换，线路利用率高  
- 交换节点支持 *差错控制*（通过校验技术）  

相比于报文交换，分组交换改进了如下问题：  
- 分组定长，方便存储转发管理  
- 分组的存储转发时间开销小，缓存开销小  
- 分组不易出错，重传代价低  

分组交换的缺点：
- 相比于报文交换，控制信息占比增加  
- 相比于电路交换，依然存在存储转发时延  
- 报文被拆分为多个分组，传输过程中可能出现失序、丢失等问题，增加处理的复杂度

== 计算机网络性能指标

*速率*：又名数据率、比特率、数据传输速率  

*带宽*：表示某信道允许通过的最高数据率  

*吞吐量*：描述某个网络（或节点、信道）的在单位时间内通过的实际数据量（实际的综合数据率）  

*带宽（另一种含义）*：表示某信道允许通过的信号频带范围，单位 Hz  

*单位*
- bit/s = b/s = bps  
- bps、kbps、Mbps、Gbps、Tbps（$10^3 eq 1000$ 递增）  

*注意：1B = 8 b*
- B、K、M、G、T（$2^10 eq 1024$ 递增）  

#pagebreak()

=== 时延

时延由四部分组成
1. 发送时延
2. 传播时延
3. 处理时延
4. 排队时延

发送时延是从发送数据帧的第一个比特算起，到该帧的最后一个比特发送完毕所需的时间

$ "发送时延" = "数据帧长度(bit)" / "发送速率(bit/s)" $

传输时延是*电磁波*在信道中传播一定的距离需要花费的时间

$ "传输时延" = "信道长度（米）" / "信号在信道上的传播速率（米/秒）" $

*错误说法*：在高速链路（或高带宽链路）上，比特会传送得更快些（这里只减少发送时延，其他时延没有考虑）

时延带宽积

$ "时延带宽积" = "传播时延" times "带宽" $

下面的习题值得一看

#image("img/q.png")

#pagebreak()

== 计算机体系结构

ISO/OSI

- OSI 的专家们在完成 OSI 标准时*没有商业驱动力*；
- OSI 的协议实现起来*过分复杂*，且运行效率很低；
- OSI 标准的制定*周期太长*，使得按 OSI 标准生产的设备无法及时进入市场；
- OSI 的*层次划分也不太合理*，有些功能在多个层次中重复出现。

结构一览

#image("img/arch.png")

#pagebreak()

= 第二章: 物理层

*1. 曼彻斯特编码*
重点在于每个码元中间必有一次跳变。其作用是用来表示数据，并提供同步时钟。

*2. 传输介质*
+ 双绞线：主要作用是降低电磁干扰（注意：是降低电磁干扰，不是降低电阻！）。
+ 同轴电缆：抗干扰好，但目前已不是主流局域网介质。
+ 光纤（重点对比）：
  - 单模光纤：芯径小，使用激光器，适合长距离传输。
  - 多模光纤：芯径大，使用LED。

*3. 码片设计*
核心原理是码片向量正交内积为0。示例：`(+1,+1,-1,+1)` 与 `(+1,-1,+1,+1)` 相乘求和的结果是 0。

*4. 时延计算*
需要掌握两个核心公式：
+ 发送时延 = 数据长度 ÷ 带宽
+ 传播时延 = 距离 ÷ 传播速度

*5. 香农公式*
+ 码间串扰：接收端收到的信号波形失去了码元之间的清晰界限。
+ 奈氏准则：码元传输的最高速率 = 2W（码元/秒）。
+ 信噪比：信号的平均功率 $S$ 和噪声的平均功率 $N$ 之比，记为 $S/N$，用分贝（dB）度量。
  - 计算公式：#strong[信噪比(dB) = $10 log 10(S/N)$]
  - 示例：当 $S/N = 10$时，信噪比就是 10dB；当 $S/N = 1000$ 时，信噪比为 30dB。
+ 信道的极限信息传输速率 C：
  - 公式：$C = W log 2(1 + S/N) "(bit/s)"$ （其中 $W$ 为带宽Hz，$S$ 为信号平均功率，$N$ 为高斯噪声功率）。

*6. 复用技术*
+ FDM (频分复用)：应用于广播电视。
+ TDM (时分复用)：应用于PCM电话。
+ WDM (波分复用)：应用于光纤通信。
+ CDM (码分复用)

= 第三章: 数据链路层

三个基本问题

1. 封装成帧：如何让接收方能够确定帧的界限
2. 透明传输：接收方要能够去除"帧定界"的附加信息，把帧"恢复原貌"
3. 差错控制：发现或“发现+解决”一个帧的内部位错

== 封装成帧

封装成帧 (framing)：在一段数据的前后分别添加首部和尾部，构成一个帧。

#image("img/framing.png")

== 透明传输

透明传输的思想是SOH与EOT是规定好的，所有数据段可能会出现SOH和EOT，于是要填充字符使得不会错误解析

#image("img/idea.png")

零比特填充
- 发送方：对帧的数据部分进行处理，每当遇到连续5个1，就填充一个0
- 接受方：对帧的数据部分进行逆处理，每当遇到连续5个1，就删掉后面的0

#image("img/zero-bit.png")

#pagebreak()

== 差错检测

奇偶校验码仅能检测出奇数位错误，且*无纠错能力*

crc冗余码的工作流如下

#image("img/crc.png")

下面是运算例子

#image("img/example-crc.png")

海明码不看

#pagebreak()

== PPP协议

*透明传输*：采用*字节填充*。三个特殊字符为 `SOH`、`EOT`、`ESC`。处理方法：当出现 `ESC` 时，再插入一个 `ESC`。

状态图如下：

#image("img/ppp.png")

== CSMA/CD

CD: Collision Detection 冲突检测

最早的以太网：将许多计算机都连接到一根总线上

problem：多个站点同时发送时，会产生发送碰撞或冲突，导致发送失败。

口诀：“先听后发，边听边发，冲突停发，随机重发”

#image("img/csma-cd.png")

== Mac地址

Mac地址是指局域网上的每一台计算机中固化在适配器的 ROM 中的地址。 Mac地址是6字节，48比特

== 交换机

交换机自学习步骤

#image("img/self-learning.png")

建议跟着ppt里面的例子练习

#image("img/image.png")

*注意下面语句里的“一致则丢弃”*

取出目的MAC。若表内无此地址，向除进入接口外的所有接口泛洪转发。*若有此地址，检查其对应接口是否与帧进入的接口一致*：一致则丢弃；不一致则向指定接口转发。

注意广播会广播所有接口，同一个集线器下能够发送，但是还会到达交换机，交换机会丢弃，因为是目标是同一个接口

#pagebreak()

= 第四章：网络层

== CIDR与ip基础

CIDR记法（相关计算要掌握），这里不多赘述

这里记得前面的分组交换的图，不要搞乱了

#image("img/ip1.png")

== ARP

ARP 用于解决同一个局域网上的主机或路由器的 IP 地址和 MAC地址的映射问题。

#image("img/arp.png")

如果不在同一个局域网上，这个时候路由器相当于中间代理了

== ip数据报

#image("img/ip2.png")

*首部结构*：包含版本、首部长度、区分服务、总长度、标识、标志、片偏移、生存时间(TTL)、协议、首部校验和、源/目的地址等。

*关键字段*：
+ 标识 (16位)：用于生成IP数据报的唯一标识。
+ 片偏移 (13位)：指出分片在原始报文中的位置，*偏移单位固定为 8 字节*。
+ 标志 (3位)：
  - MF (最低位)：MF=1表示后面还有分片，MF=0表示是最后一片。
  - DF (中间位)：只有DF=0时才允许分片。

== 最长前缀匹配 (longest-prefix matching)

使用 CIDR 时，在查找转发表时可能会得到不止一个匹配结果。

原则：选择前缀最长的一个作为匹配的前缀（说明这个选择要查找的范围最小，是最好的选择）

== icmp

ICMP (Internet Control Message Protocol) 允许主机或路由器报告差错情况和提供有关异常情况的报告。

`ping` 命令就是icmp的应用之一

#pagebreak()

== 路由协议

协议大体如下：

#image("img/router-1.png")

=== rip

对每个相邻路由器（假设其地址为 X）发送过来的 RIP 报文

1. 修改 RIP 报文中的所有项目（即路由）：把“下一跳”字段中的地址都改为 X，并把所有的“距离”字段的值加 1。
2. 对修改后的 RIP 报文中的每一个项目，重复以下步骤：
   - 若路由表中没有目的网络N，则把该项目添加到路由表中。否则
     - 若路由表中网络 N 的下一跳路由器为 X，则用收到的项目替换原路由表中的项目。否则
       - 若收到项目中的距离小于路由表中的距离，则用收到项目更新原路由表中的项目。否则什么也不做。
3. 若 3 分钟还未收到相邻路由器的更新路由表，则把此相邻路由器记为不可达路由器，即将距离置为 16（表示不可达）。
4. 返回。

RIP 协议特点：好消息传播得快，坏消息传播得慢。

缺点是坏消息传播得慢

=== ospf

链路状态路由协议，底层使用 *Dijkstra (迪杰斯特拉) 算法* 计算最短路径树。

于是要学习整个区域的信息

#pagebreak()

= 第五章：运输层

*这一章最重要*

== UDP

UDP 只在 IP 的数据报服务之上增加了一些功能：
1. 复用和分用
2. 差错检测

特点
1. *无连接*。发送数据之前不需要建立连接。
2. *使用尽最大努力交付*。即不保证可靠交付。
3. *面向报文*。UDP 一次传送和交付一个完整的报文。
4. *没有拥塞控制*。网络出现的拥塞不会使源主机的发送速率降低。很适合多媒体通信的要求。
5. 支持一对一、一对多、多对一、多对多等交互通信。
6. *首部开销小*，只有 8 个字节。

== TCP

socket

$ "套接字 socket" = ("IP地址" : "端口号") $

=== 停止等待协议

每发送完一个分组就停止发送，等待对方的确认。在收到确认后再发送下一个分组。

优点：简单。缺点：信道利用率太低。

$ "信道利用率" U = T_D / (T_D + "RTT" + T_A) $

=== 滑动窗口

这里其实建议通读ppt理解

首先是三次握手

#image("img/three-handshake.png")

#pagebreak()

然后是四次挥手

#image("img/goodbye_1.png")

等待2MSL的意义
1. 保证发送的最后一个 ACK 报文段能够到达 B。
2. 防止“已失效的连接请求报文段”出现在本连接中。

==== 拥塞控制

*四种算法(RFC 5681)*
- 慢开始 (slow-start)
- 拥塞避免 (congestion avoidance)
- 快重传 (fast retransmit)
- 快恢复 (fast recovery)。

这里介绍FR

当发送端收到连续三个重复的确认时，不执行慢开始算法，而是执行快恢复算法 FR (Fast Recovery) 算法：
1. 慢开始门限 ssthresh = 当前拥塞窗口 cwnd / 2 ；
2. 乘法减小 MD (Multiplicative Decrease) 拥塞窗口。新拥塞窗口 cwnd = 慢开始门限 ssthresh ；
3. 执行拥塞避免算法，使拥塞窗口缓慢地线性增大（加法增大 AI）。

建议看ppt学习整个过程

#image("img/exmaple.png", height: 26.8%, width: 100%, fit: "contain")

#image("img/tcp_control.png")

TCP超时重传与RTT计算
+ 加权平均往返时间 ($"RTT_S"$)：
  - 公式：新的 $"RTT_S" = (1 - alpha) times "旧的RTT_S" + alpha times "新的RTT样本"$ ，其中RFC 6298推荐 $alpha = 1/8$。
+ 超时重传时间 ($"RTO"$)：需略大于 $"RTT_S"$。公式：$"RTO" eq "RTT_S" + 4 times "RTT_D"$（$"RTT_D"$为RTT偏差的加权平均）。
+ RTT偏差 ($"RTT_D"$)：
  - 公式：新的 $"RTT_D" = (1 - beta) times "旧的RTT_D" + beta times |"RTT_S" minus "新的RTT样本"|$，RFC 6298推荐 $beta = 1/4$



#pagebreak()

= 应用层

+ DNS：重点掌握递归查询和迭代查询。
+ FTP：控制连接使用 21端口，数据连接使用 20端口。
+ URL格式：标准为 `协议://主机:端口/路径`。
+ DHCP：牢记 DORA 四步流程，即 `Discover` (发现) → `Offer` (提供) → `Request` (请求) → `ACK` (确认)。
+ SMTP / HTTP：用于邮件服务器之间的邮件传输。
+ 加密机制对比：
  - 对称加密：速度极快，适合大数据加密。代表算法：AES、DES。
  - 非对称加密：速度慢，用于密钥交换、数字签名。代表算法：RSA。

下面只对DNS和DHCP进行笔记记录

== DNS查询

首先是迭代查询
1. 本地域名服务器向根域名服务器查询时使用。
2. 要么给出所要查询的 IP 地址，要么告诉下一个要查询的域名服务器的 IP 地址。
3. 本地域名服务器继续后续查询。

#image("img/recursive.png", fit: "stretch", height: 34%, width: 105%)

然后是递归查询
1. 通常，主机向本地域名服务器查询时使用。
2. 若不知道，就以 DNS 客户的身份，向其他根域名服务器继续发出查询请求报文。

#image("img/rec.png", fit: "stretch", height: 22%, width: 72%)


== DHCP

给协议参数赋值的动作叫做协议配置。

连接到互联网的计算机的协议软件需要正确配置的参数包括：
1. IP 地址
2. 子网掩码
3. 默认路由器的 IP 地址
4. 域名服务器的 IP 地址

这里其实和计算机网络实验的DHCP协议分析有做过

四步：`Discover` (发现) → `Offer` (提供) → `Request` (请求) → `ACK` (确认)。
1. *Discover（发现）*：客户端启动时，由于没有IP地址，会以*广播*方式（目标IP `255.255.255.255`，源IP `0.0.0.0`）发送一个 `DHCP Discover` 报文，寻找网络中的DHCP服务器。
2. *Offer（提供）*：当DHCP服务器收到Discover报文后，会从自己的IP地址池中选择一个可用的IP地址，然后以*广播或单播*的方式向客户端发送一个 `DHCP Offer` 报文。这个报文中包含了提供的IP地址以及其他网络配置信息。
3. *Request（请求）*：如果网络中有多个DHCP服务器，客户端通常会选择*第一个*收到的Offer。它会再次以*广播*方式发送 `DHCP Request` 报文，明确表示接受该服务器的IP地址租约。
4. *Acknowledgment（确认）*：被选中的DHCP服务器收到Request报文后，会发送一个最终的 `DHCP Ack` 确认报文。客户端收到此报文后，会正式使用分配的IP地址。

最后是地址租约 (Lease)的内容
1. 续租 (Renewal)：客户端会在租期达到50%时，尝试向原服务器单播发送Request报文进行续租。
2. 重新绑定 (Rebinding)：如果50%时续租失败，客户端会在租期达到87.5%时，向所有服务器广播发送Request报文
